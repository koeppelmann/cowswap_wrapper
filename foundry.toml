[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.34"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"            # EIP-1153 transient storage

[fmt]
line_length = 120
